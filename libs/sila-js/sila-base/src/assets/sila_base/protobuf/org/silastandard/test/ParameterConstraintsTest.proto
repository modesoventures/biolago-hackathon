// This file is automatically generated by sila2lib.proto_builder.proto_builder version 0.2.0
// :generation date: 2021-10-08 10:53:38
//
// ---- PLEASE DO NOT MODIFY MANUALLY !! ---

syntax = "proto3";
import "SiLAFramework.proto";
package sila2.org.silastandard.test.parameterconstraintstest.v1;

// Feature: Parameter Constraints Test
//   
//           This is a test feature to generically test constrained parameters.
//           A validation Error must be raised if the constraints are not fulfilled.
//   
service ParameterConstraintsTest {
    // Exact String Length
    //   Tests the Length constraint on String type
    rpc ExactStringLength(sila2.org.silastandard.test.parameterconstraintstest.v1.ExactStringLength_Parameters) returns (sila2.org.silastandard.test.parameterconstraintstest.v1.ExactStringLength_Responses) {}
    // Constrained Numeric Parameters
    //   A command with constrained parameters of type Integer or Real
    rpc ConstrainedNumericParameters(sila2.org.silastandard.test.parameterconstraintstest.v1.ConstrainedNumericParameters_Parameters) returns (sila2.org.silastandard.test.parameterconstraintstest.v1.ConstrainedNumericParameters_Responses) {}
    // Constrained Date and Time Parameters
    //   A command with constrained parameters of type Date, Time or Timestamp
    rpc ConstrainedDateTimeParameters(sila2.org.silastandard.test.parameterconstraintstest.v1.ConstrainedDateTimeParameters_Parameters) returns (sila2.org.silastandard.test.parameterconstraintstest.v1.ConstrainedDateTimeParameters_Responses) {}
    // Constrained List Parameters
    //   A command with constrained parameters of type List
    rpc ConstrainedListParameters(sila2.org.silastandard.test.parameterconstraintstest.v1.ConstrainedListParameters_Parameters) returns (sila2.org.silastandard.test.parameterconstraintstest.v1.ConstrainedListParameters_Responses) {}
    
    
}

// ----------------- Data Type definitions -----------------


// ------ Command Parameter and Response definitions -------
message ExactStringLength_Parameters {
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.String StringLength = 1;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.String MaximalLengthString = 2;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.String MinimalLengthString = 3;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.String MinMaxLengthString = 4;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.String SetString = 5;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.String PatternString = 6;
}

message ExactStringLength_Responses {
    
}

message ConstrainedNumericParameters_Parameters {
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Integer IntegersSet = 1;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Real MaximalExclusiveReal = 2;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Real MinimalExclusiveReal = 3;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Real MinMaxExclusiveReal = 4;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Integer MaximalInclusiveInteger = 5;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Integer MinimalInclusiveInteger = 6;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Integer MinMaxInclusiveInteger = 7;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Integer Unit = 8;
}

message ConstrainedNumericParameters_Responses {
    
}

message ConstrainedDateTimeParameters_Parameters {
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Date DateSet = 1;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Date MaximalExclusiveDate = 2;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Date MinimalExclusiveDate = 3;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Date MinMaxExclusiveDate = 4;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Time MaximalInclusiveTime = 5;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Time MinimalInclusiveTime = 6;
    // Constrained type, not reflected in protocol buffers
    sila2.org.silastandard.Time MinMaxInclusiveTime = 7;
}

message ConstrainedDateTimeParameters_Responses {
    
}

message ConstrainedListParameters_Parameters {
    // Constrained type, not reflected in protocol buffers
    repeated sila2.org.silastandard.String ElementCountList = 1;
    // Constrained type, not reflected in protocol buffers
    repeated sila2.org.silastandard.String MaximalElementCountList = 2;
    // Constrained type, not reflected in protocol buffers
    repeated sila2.org.silastandard.String MinimalElementCountList = 3;
    // Constrained type, not reflected in protocol buffers
    repeated sila2.org.silastandard.String MinMaxElementCountList = 4;
}

message ConstrainedListParameters_Responses {
    
}


// ----------------- Metadata Definitions ------------------
